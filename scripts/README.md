# Scripts

This folder is reserved for host automation scripts.

Good candidates:

- Serial smoke test script that opens the CDC port and runs `help`, `status`, and protocol probes.
- Release script that builds firmware and copies UF2 artifacts.
- Documentation build script for `docs/latex/manual.tex`.

No scripts are required for the current firmware build.
